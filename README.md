# xmip-core-route-metadata

Metadata route technology: `metadata:generation`, `created-by`, `sections`, `size`, `priority`, `durability` and `id` read what the Message knows about itself. A technology of [xmip-core-route](https://github.com/IlleNilsson/xmip-core-route).

## Toolchain

`rust-toolchain.toml` pins the toolchain for the whole estate. Do not change it
here.

## Verification

The included workflow is manual-only and calls the versioned shared workflow at
`IlleNilsson/.github@v1`.
